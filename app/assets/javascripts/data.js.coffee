define ['jquery'], ($) ->
	# this is the "global" data model layer that persists objects b/w calls
	# see http://stackoverflow.com/questions/10148778/persistent-accessible-backbonejs-models-with-requirejs
	# include Data anywhere a module needs persistent values, just add to Data variable via Data.obj = obj

	Data = ->

	Data